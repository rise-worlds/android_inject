{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build task",
            "type": "shell",
            "windows": {
                "command": "${workspaceFolder}/debug.ps1"
            }
        }
    ]
}